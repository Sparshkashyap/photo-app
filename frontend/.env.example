# Base URL of the backend API (API Gateway stage URL).
# Leave unset in development to run the frontend against the local mock layer.
VITE_API_BASE_URL=https://y181ertste.execute-api.ap-south-1.amazonaws.com





